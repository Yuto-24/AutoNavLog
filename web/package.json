{
  "name": "autonavlog-web",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:release-notes": "node scripts/generate-release-notes.mjs",
    "predev": "npm run generate:release-notes",
    "prebuild": "npm run generate:release-notes",
    "pretypecheck": "npm run generate:release-notes",
    "dev": "vite --host 0.0.0.0",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b --pretty false",
    "test:release-notes": "node --test scripts/generate-release-notes.test.mjs",
    "test:e2e": "playwright test",
    "dev:local": "VITE_CALCULATION_MODE=local vite --host 127.0.0.1 --port 5174",
    "build:local": "npm run generate:release-notes && VITE_CALCULATION_MODE=local tsc -b && VITE_CALCULATION_MODE=local vite build --outDir dist-local",
    "prepare:local": "python3 ../scripts/prepare_local.py",
    "prebuild:local": "npm run prepare:local",
    "predev:local": "npm run generate:release-notes && npm run prepare:local",
    "test:local": "playwright test --config playwright.local.config.ts --grep 'static FTD Golden, no API fallback, 1100px|actual MSM FORECAST reaches NAV LOG with Python provenance and no fallback|portable real MSM: cold, warm after reload, Python reference and saved NAV LOG after eviction|Latest replacement, promotion, opening another Project and explicit delete|a stale selector cannot delete a Project updated by another tab|reload preserves raw invalid planning values, import and route selection|new discards recovery; new tabs including opener clones never inherit work|picker opens compressed KMZ; drop replaces content; cancelled picker keeps the import|(?<!un)supported Clipboard preserves click activation and manual paste recovery|source links open without an opener and cannot unlock a pending save'",
    "test:local:full": "playwright test --config playwright.local.config.ts",
    "test:application": "playwright test --config playwright.application.config.ts",
    "test:auth": "playwright test --config playwright.auth.config.ts --grep 'anonymous workflow, account switch, logout, reauthentication and cross-tab closure|Account dialog remains compact and workflow regions retain their order|different identity closes the prior Application before async mapping, token refresh preserves it|failed persistent logout closes access and offers a visible retry in the new context|blocked Google popup reports an actionable error without resetting anonymous input'",
    "test:auth:full": "playwright test --config playwright.auth.config.ts",
    "test:taf": "playwright test --config playwright.taf.config.ts",
    "pretest:sync": "npm run generate:release-notes",
    "test:sync": "playwright test --config playwright.sync.config.ts",
    "test:firestore": "playwright test --config playwright.firestore.config.ts"
  },
  "dependencies": {
    "@fontsource-variable/noto-sans-jp": "5.3.0",
    "comlink": "4.4.2",
    "firebase": "12.19.0",
    "leaflet": "1.9.4",
    "lucide-react": "1.30.0",
    "pyodide": "0.27.7",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "react-leaflet": "5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/leaflet": "1.9.22",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.5",
    "fake-indexeddb": "6.2.5",
    "typescript": "7.0.2",
    "vite": "8.2.1"
  }
}
