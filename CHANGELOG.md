# 変更履歴

AutoNavLog のRelease履歴です。公開日時はGitHub Releaseを正本とします。

## 1.12.0

### 利用者向け

#### 改善

- 同じタブの再読み込み後に、作業中の経路・入力途中の値・NAV LOG編集と直前の計算結果へ戻れるようにしました。計算中の再読み込みでは計算を再開しません。
- 別タブの未保存作業を分離し、「新規」で閉じた作業は再読み込み後に復元しません。

### 開発者向け

- Legacy / Local共通のFrontend Application Sessionを追加しました。タブ内の一時的なreload recoveryと永続Project保存を分離し、Local Persistenceは引き続き#124の責務です。

## 1.11.7

### 利用者向け

#### 改善

- 内部処理を改善しました。AutoNavLogの通常の使い方に変更はありません。

### 開発者向け

- UIを共通のApplication操作へ接続し、Legacy HTTPとLocal Browserの通信方式・失敗情報の変換をAdapterへ分離しました。既存の編集・自動保存・再計算とPython Coreを維持し、Local永続化は含みません。
- request入力検証と内部処理の失敗を分離し、Legacy/Localの内部エラーを同じcodeと固定メッセージへ正規化しました。

## 1.11.6

### 利用者向け

#### 改善

- 内部処理を改善しました。AutoNavLogの通常の使い方に変更はありません。

### 開発者向け

- Pyodide Local Calculationで固定MSM由来データによるFORECASTを実行可能にしました。FTDと同じCalculation Coreを使用し、既存Run固定・RJFM例外・手動入力・Check PointをPython Referenceと比較します。
- Local wheelの正式インストールとassetハッシュ検証を追加し、Pyodide同梱PydanticでRJFM状態を厳格に復元できるようにしました。通常経路の切替、MSM/TAF本番取得、Local永続化は含みません。

## 1.11.5

### 利用者向け

#### 改善

- 内部処理を改善しました。AutoNavLogの使い方に変更はありません。

### 開発者向け

- 開発・検証用のPyodide Local PoCを追加。FTD気象を使い、KML取込からNAV LOG表示までブラウザ内で実行できます。入力は再読込で失われます。

## 1.11.4

### 利用者向け

#### 修正

- 地図に「403 Access blocked」が表示される問題に対応するため、OpenStreetMapの標準タイル取得先を更新しました。

### 開発者向け

- Issue #163で、OSM TileLayerのURLを公式指定の `https://tile.openstreetmap.org/{z}/{x}/{y}.png` に変更しました。CSPの画像許可先も同ホストへ合わせ、既存LeafletのreferrerPolicyでタイル要求にoriginのRefererを送ります。既存のattributionは維持しています。

## 1.11.3

### 利用者向け

#### 改善

- RJFMへの到着ルートを計算する際の待ち時間を短縮しました。

### 開発者向け

- Issue #154で数値回帰5件をprofileし、境界検証の各区間で使わない測地線中点の計算を除去しました。探索条件・golden・数値toleranceは維持し、計測手順と結果を`docs/testing.md`へ記録しました。
- 通常の生成経路も確認対象の保存済みデータも使用していなかった`RouteNodeRole.TURN_POINT`を削除し、テスト入力を`ROUTE_POINT`へ統合しました。互換変換は追加していません。

## 1.11.2

### 利用者向け

#### 改善

- リリース管理を整理しました。AutoNavLogの使い方に変更はありません。

### 開発者向け

- Issue #152で、root `VERSION` と `CHANGELOG.md` をRelease情報の唯一の原本にし、PRのRelease契約とGitHub Release生成をこの形式へ移行しました。

## 1.11.1

### 利用者向け

#### 改善

- 内部処理を改善しました。AutoNavLogの使い方に変更はありません。

### 開発者向け

- Issue #141で、PRブランチのpushとpull requestによる同一commitの重複CIを解消し、同じPRまたはbranchの古い実行を自動cancelするようにしました。

## 1.11.0

### 利用者向け

#### 改善

- 「KMLを貼り付け」を押すだけで、コピーした経路を読み込めるようになりました。読み取れない場合は入力欄に貼り付けて取り込めます。

### 開発者向け

- Issue #85で、KML貼り付け操作からClipboard APIで直接取り込み、読取不可時の手動入力と取り込みエラーからの再試行に対応しました。

## 1.10.0

### 利用者向け

#### 追加

- 画面上部のInformationアイコンから、お知らせと過去の更新内容を確認できるようになりました。
- 新しいお知らせがあるとアイコンに印が付きます。利用前に知っておきたい不具合の追加や内容変更は、黄色の印でお知らせします。
- 画面が狭いときも、プロジェクト名と保存・読込などの操作を2行以内にまとめて表示します。

### 開発者向け

- Issue #130で、InformationにRELEASE_NOTESとKNOWN_ISSUESを表示し、可視内容のhashで既読を管理します。Known Issueの追加・本文更新は通常更新と区別して通知します。
- Headerを明示的な最大2行レイアウトにし、Information・保存・新規を狭い幅でアイコン表示にします。
- change fragment、厳格な情報ソース検証、Docker release-toolsとprepare_release.pyによる一括リリース準備を導入します。

## 1.9.5

### 利用者向け

#### 修正

- 降下を始める位置が変わったとき、計算に使う速度が正しく更新されるようにしました。
- 手入力した速度が、別の区間の計算にも使われてしまう問題を修正しました。

### 開発者向け

- Issue #136で、EOC直前の実効CRUISE Calculation ZoneのCASを、以後のDESCENT全体の
  共通CASとして自己整合的に採用するようにしました。
- Issue #137で、RCA/EOCで分割されたPhysical Legの手動TASをeffective Flight Phase別に
  保存・適用し、CLIMB、CRUISE、DESCENT間へoverrideが漏れないようにしました。

## 1.9.4

### 利用者向け

#### 改善

- 内部処理を改善しました。AutoNavLogの使い方に変更はありません。

### 開発者向け

- Issue #79で、backendの静的検査、テスト、データ・schema検証、配布buildをDockerのtest stageで
  実行するCIへ移行しました。本番runtime imageはtest依存と検査用ファイルを含めず、既定のMSM
  起動設定で`/healthz`へ応答することもCIで確認します。

## 1.9.3

### 利用者向け

#### 修正

- 保存した飛行計画の日付や出発時刻を変えたとき、新しい時間帯に合う気象予報で計算するようにしました。

### 開発者向け

- Issue #133で、保存済みProjectのDATEまたはETDを変更したときは、旧日時に固定された
  Forecast Runを解除して、更新後の飛行時間帯に適合するRunを再選択するようにしました。
  日時・気象設定を変更しない編集では既存のForecast Runを維持します。

## 1.9.2

### 利用者向け

#### 改善

- NAV LOGの横スクロール中も区間名を確認できるようにし、総距離・総時間・燃料計画を見やすく整理しました。
- 地上の無線施設を基準に位置を確認するための選択欄を改善し、同じ施設が重複して選ばれるのを防ぎました。
- 宮崎空港へ到着する経路で、一部の地点の高度を自動設定するようにしました。手入力した高度はそのまま使います。

### 開発者向け

- Issue #103で、宮崎到着のARITA／有田・SHIRAHAMA／白浜のV-REP自動高度を1500 ft MSLにしました。名称または基準点から0.5 NM以内で識別し、手動高度は維持します。
- Issue #93・#104で、NAV LOGの数値列幅を縮小し、横スクロール時にFROM/TO列を固定しました。
- Issue #91で、VOR/DME基準局の自動・手動選択の重複を防ぎ、変更・解除した局を候補へ即時反映するようにしました。
- Issue #132で、TTL DIST・TTL TIMEと目的地風を主表上部のSummaryにまとめ、TIME / FUEL PLANを主表の下の独立したセクションへ移しました。既存の計算値・表示丸めを維持します。

## 1.9.1

### 利用者向け

#### 修正

- 取り込んだ経路の宮崎空港や「小丸」を正しく認識し、地点名を保ったまま表示するようにしました。

### 開発者向け

- Issue #129で、RJFM認識を座標ベースにし、OMARU参照に明示された「小丸」Pointを
  取込名のまま単一の経路ノードとして保持するよう修正しました。

## 1.9.0

### 利用者向け

#### 追加

- 入力中の飛行計画を自動保存し、最新の計画を一覧から開けるようにしました。名前を付けて保存した計画も残ります。
- 最後に正常に計算できたNAV LOGを保存し、画面を開き直しても確認できるようにしました。入力を変更した後は、変更前の計算結果であることを表示します。
- 次に使うとき、最後に作成・選択した飛行計画を開くようにしました。

### 開発者向け

- Issue #94で、serverが受理したProject入力を最新draftとして自動保存し、明示保存していない
  Projectも一覧・読込・削除できるようにしました。計算前の飛行条件・高度・PHASE編集も300 ms後に
  保存し、不正な編集中フィールドは直前の正常値を維持します。明示保存・計算は入力検証とdraft保存の
  完了後だけ開始し、自動保存失敗は画面へ表示します。
- autosave-only Projectはownerごとに最新1件だけを`Latest`として一覧表示し、新しいProjectの
  autosave成功後に旧draftを自動削除します。明示保存したProjectは名前付きcheckpointとして残り、
  通常autosaveではrevisionを変更しません。named volumeを維持した更新・container再作成後も、
  最新draftとowner状態を復元します。
- Projectごとに最後に正常完了したNAV LOG、計算時点のProject、目的地風、Forecast Run・来歴を
  1件だけ保存し、reload、Cookie消失、server再起動後にも復元します。入力変更後は直前のNAV LOGを
  stale表示で維持し、失敗・中断・Blockerのある計算では置き換えません。
- ownerごとに最後に明示作成・選択したProjectを復元し、owner identityを生のまま保存pathへ含めない
  分離を追加しました。

## 1.8.3

### 利用者向け

#### 修正

- 宮崎の訓練空域の地図に、実際にはない境界線が表示される問題を修正しました。

### 開発者向け

- Issue #114で、RJFMの民間訓練試験空域KS4は、GSI GeoJSONのPolygonを塗りつぶし、明示されたLineStringを
  実境界線として分離して検証・表示するよう修正しました。タイル境界で分割されたPolygonの
  切断端を境界線として誤表示せず、取得失敗や形式不一致時は両レイヤーを非表示にします。

## 1.8.2

### 利用者向け

#### 改善

- 性能表に必要な値がない場合の計算方法を見直しました。速度・所要時間・燃料の計算結果が変わることがあります。
- 性能表の範囲を超える条件で計画したとき、確認が必要な区間を「準備状況」で分かりやすく表示するようにしました。

### 開発者向け

- 巡航性能表で65% PWRを挟む行がないPA/ISA cornerは、表端へclampせず最寄り2行から65%へ
  線形外挿します。これはAutoNavLogの実装Policyであり、該当LegのKTAS、GPH、ETE、燃料が
  変わることがあります。
- 表端の高度・ISA採用は準備状況の確認事項へ残し、その画面ではCalculation conditionと
  interpolation cornerを分離して表示します。来歴は軸共通のprovenanceとして保持し、
  Readiness APIにはboundary詳細と人間可読なLeg/Zone位置を加算的に公開します。
- 65% PWR外挿そのものはWarningにせず、支持する2行・係数・65%の解決値を計算metadataへ
  残して監査できるようにしました。

## 1.8.1

### 利用者向け

#### 追加

- 宮崎空港を到着地にした場合も、周辺の訓練空域などを地図に表示するようにしました。

### 開発者向け

- RJFM到着空港でも訓練領域（PCA/民間訓練試験空域）を地図上に表示するための判定を拡張しました。
  到着RJFMを選ぶ既存Projectの表示互換性を維持しつつ、表示範囲を拡張しました。

## 1.8.0

### 利用者向け

#### 修正

- NAV LOGの細かい区間の表示距離を足すと、全体の表示距離に一致するようにしました。
- 距離を正しく確認できない場合は、推測した値ではなく「未取得」と表示するようにしました。

### 開発者向け

- 分割されたNAV LOGのDIST表示では、Physical Legの採用距離を先に0.5 NM単位へ丸め、
  子Calculation Zoneへ最大剰余法で配賦するよう修正しました。親ZONE/CUM DISTと子DISTの
  表示算術を一致させつつ、計算結果のexact距離、ETE、燃料、REMは変更しません。
- 採用距離と子Zone距離が欠損または不整合の場合、DISTを推測表示せず`未取得`として扱います。

## 1.7.1

### 利用者向け

#### 改善

- 宮崎空港へ戻る経路の計算を速くしました。

#### 修正

- Safariで地図を指2本で拡大・縮小したとき、ページ全体まで拡大される問題を修正しました。

### 開発者向け

- Safari で地図内の pinch gesture がページ全体の拡大へ渡らないよう、経路地図 frame と
  Leaflet surface に限定して `touch-action: none` を適用し、frame の scroll chaining を
  `overscroll-behavior: contain` で抑制しました。ページ全体の zoom 制限や JavaScript の
  gesture 抑制は追加していません。
- RJFM帰路の production reference で使う境界判定と測地 sampling を、探索予算、0.5 NM
  sampling、guard/model-error、数値結果を変えずに高速化しました。

## 1.7.0

### 利用者向け

#### 追加

- 宮崎空港へ戻る一部の経路で、降下の計画と経路を延長するための案内に対応しました。

#### 修正

- 降下が複数の区間にまたがる場合の計算を修正しました。
- 降下を始めた後の風や速度を表示し、巡航時からの変化を確認しやすくしました。

### 開発者向け

- RJFM帰路で物理座標のOMARU→UMK→最終VREPを認識した場合、OMARU→UMKを4,500 ft固定・編集不可にし、UMKをEOCとして扱う運用降下profileを追加しました。物理DIST/TC/MC/CUM DISTは変更せず、UMK→VREPのETEと12 GPH燃料だけを選択降下率による運用時間へ置き換えます。
- UMK後の西方経路延長solverとMZE DME案内を追加しました。丸め前後のTurn Point、DME、追加距離、予測ETE、KS4-3余裕を現在の計算結果だけに表示し、案内失敗・参照検証失敗はNAV LOGを止めないwarning-only扱いにします。同梱したAIP Japan ENR 5.3-21の一次水平境界は、元PDF SHA-256、PDFページ、公開DMS頂点、決定的なWGS84 arc正規化、最大モデル誤差を検証してからsolverに渡します。

- EOCが前の物理Legへ移動する場合も、EOCからVREPまではDESCENT基準Legの風を共通適用し、
  各LegのTCに応じてWCA、MH、GS、EOC位置、ETEを再計算するよう修正しました。
- EOC直後のDESCENT Zoneでは、風の切替を明確にするためWIND、WCA、MH、GSを明示表示するよう
  修正しました。物理Leg途中と変針点へsnapしたEOCの両方に適用し、以後の通常継承は維持します。

## 1.6.3

### 利用者向け

#### 修正

- タッチアンドゴーの回数を消して入力し直せるようにしました。
- NAV LOGの燃料合計・残量や時間が、画面に表示された内訳の足し引きと一致するようにしました。
- 画面に表示された進行方向に合う巡航高度が、候補に出るようにしました。

### 開発者向け

- TGLを編集中は空欄にできる文字列Draftとし、確定時に0～20の整数として検証してから
  APIへ数値で送るよう修正しました。モバイルを含む数値入力の全消去・再入力を共通設計として
  文書化しました。
- NAV LOGの親SECT FUELを丸め済み子表示値の合計、REMを表示値の逐次減算で構成し、
  Fuel PlanのFuel・Timeも表示済み内訳から再構成するよう修正しました。内部exact値は維持します。
- 巡航高度候補をNAV LOGへ表示する1°half-up後のMCで判定し、179.5°と359.5°の境界で
  表示方位と候補系列が一致するよう修正しました。

## 1.6.2

### 利用者向け

#### 修正

- NAV LOGに表示する地上の無線施設からの方位と距離を、各区間の到着点に合わせて修正しました。

### 開発者向け

- NAV LOGのVOR radialとDME距離を、各行のFROM地点ではなくTO地点を基準に
  算出・表示するよう修正しました。複数のVOR/DME列もすべてTO地点を基準にします。

## 1.6.1

### 利用者向け

#### 修正

- チェックポイントに対応する経路の候補に、適切でない区間が表示される問題を修正しました。

### 開発者向け

- チェックポイントの関連Leg候補を、abeam点がLeg線分内部にあり、横ずれが10 NM以内となる
  区間だけに限定しました。Leg端点から10 NM以内という理由だけでは候補に表示しません。

## 1.6.0

### 利用者向け

#### 変更

- 「チェックポイントを追加」を押すと、そのまま地図から位置を選べるようにしました。
- 位置を選ぶ間は地図を強調し、名前の入力が必要な場合も分かりやすく案内するようにしました。

### 開発者向け

- 「チェックポイントを追加」を押すと、地図から座標を選ぶモードを自動で開始するようにしました。
- 座標選択中は地図以外を暗くし、クリック対象の地図を強調表示するようにしました。
- 地図上の地点を選んだ後、未入力の名称欄を赤く強調し、入力が必要な理由を
  「チェックポイントの名称・未入力（入力必須）」と案内するようにしました。

## 1.5.1

### 利用者向け

#### 修正

- 計画高度に対する不要な警告を減らしました。
- 注意が必要な高度は黄色で表示し、計算を止めるエラーと区別しやすくしました。

### 開発者向け

- 経路表のALTが3,000 ft以下の場合、MC候補外でも警告しないようにしました。
- 3,000 ftを超える任意値は、Blockingと区別できる黄色の警告表示に変更しました。

## 1.5.0

### 利用者向け

#### 追加

- 降下の速さを2種類から選び、降下を始める位置・所要時間・燃料に反映できるようにしました。

#### 変更

- 以前に保存した飛行計画は、標準の降下設定で読み込みます。

### 開発者向け

- Project単位で計画降下率を500 fpm（標準）または1000 fpmから選択できるようにしました。
  選択値はEOC、降下時間、ETE、燃料、降下高度表示へ一貫して反映します。

- Project schemaをv4へ更新しました。旧Projectは計画降下率500 fpmとして読み込みます。

## 1.4.3

### 利用者向け

#### 変更

- サポートする起動方法を、指定の方法に統一しました。

#### 修正

- 到着に向けた目標高度の変更を保存し、NAV LOGや時間・燃料の再計算に反映するようにしました。

### 開発者向け

- 経路表で変更したVREP高度を手動の到着計画高度として保存し、NAV LOGと
  DESCENT／EOC、時間・距離・燃料の再計算へ一貫して反映するようにしました。
- VREP高度を変更していない場合は、距離と場周経路高度から求める従来の自動高度を維持します。

- Colab廃止後も残っていたGoogle Drive向け正式配布workflow、Driveアップロード処理、
  配布tree用manifest生成と専用テストを削除しました。
- 空港・性能のruntime data検査はDrive配布から切り離し、通常CIで継続して検査します。
- サポート対象runtimeを Docker / Docker Compose のみにし、containerのPythonを3.12に
  固定しました。hostへinstallして直接実行する方法はサポート対象外です。
- backend CI、Python package metadata、Ruffの解析対象をPython 3.12へ統一しました。

## 1.4.2

### 利用者向け

#### 修正

- 降下がひとつの区間に収まらない場合も、手前の区間を含めて降下の開始位置・高度・所要時間を計算するようにしました。
- 計画どおりに降下できない場合、見直す必要がある箇所を表示するようにしました。

### 開発者向け

- EOCはDESCENT基準Legから連続した500 fpm降下として逆算し、同Leg内に収まらない場合は
  経路始点側のLegを順に追加して再計算するようにしました。変針点ごとの高度制約は課しません。
- 経路をまたぐ降下ではLegごとのDESCENT風・TC・TASからGSを求め、VREPでlevel offした後の
  減速時間を60秒ちょうど1回だけ加えます。中間変針点のPAはEOCからの経過時間で推定表示します。
- 追加した前Legの計画高度が次Legより低い場合と、経路始点からでも不足する場合に、見直す対象を
  明示したBlockerを表示します。

## 1.4.1

### 利用者向け

#### 変更

- 経路表の地点名を広く表示し、上昇・巡航・降下の設定は「変更」を押したときだけ編集する形にしました。
- 上昇や降下に使う区間を選び直しやすくし、時間や燃料の計算にも影響することを案内するようにしました。
- 降下を始める位置の計算を見直し、必要な高度に到達できない場合は計算を止めるようにしました。

### 開発者向け

- 経路表の `PHASE` 列を通常時は非操作表示にし、列幅を縮小して `POINT` 列へ表示領域を戻しました。
- `PHASE` 列の「変更」を押した場合だけ通常区間を編集できるようにし、`CLIMB` / `DESCENT` を
  別 Leg に選ぶと既存の同 Phase を自動で `CRUISE` へ戻す基準 Leg 方式へ変更しました。
- RJFM北行き例外の固定区間と `VISUAL_ARRIVAL` は、PHASE変更モードでも固定のままとしました。
- PHASE変更中は、RCA・EOC・性能・時間・燃料計算へ影響することを経路表の直上へ表示します。
- EOCはDESCENT基準Leg自身の高度から探索を開始し、Leg内ならそこで確定、物理変針点上で
  一致する場合だけ前方Legへ再帰するよう修正しました。各変針点高度を満たせない短いLegは
  Blockerとし、Legごとの風・GSとprofile全体で1回だけのlevel off後60秒減速を使用します。

## 1.4.0

### 利用者向け

#### 変更

- ブラウザーで使う画面に一本化し、従来のColab版とGoogle Driveへの飛行計画保存を終了しました。
- A4転記補助表の出力と計算結果の保存を終了しました。飛行計画の入力内容は引き続き保存できます。
- 降下中の高度表示を見直し、目標高度に到達した後の減速を反映するようにしました。

### 開発者向け

- 配布と実行経路を Web 版へ一本化し、Colab Notebook、Preview bundle、Colab UI、Google Drive
  Project 保存を廃止しました。
- A4 転記補助 HTML と、その出力 API・画面操作・専用 presentation code を廃止しました。
- 計算結果 Snapshot の保存 API と schema を廃止し、保存対象を Project 入力へ整理しました。
- Project schema を v3 に更新し、子要素の重複 `project_id`、未使用の SEA・loss・notes・
  `along_track_fraction` を読込時に破棄します。
- 計算結果から、表示専用の重複未丸め値、未使用の ETO、反復履歴を削除しました。
- 降下高度プロファイルを「降下後に level off し、その後 1 分減速」として独立させました。
  総降下時間と EOC の位置は従来どおりです。
- 同期計算 API を廃止し、進捗を扱える calculation job API に一本化しました。

## 1.3.2

### 利用者向け

#### 追加

- NAV LOGに、地上の無線施設からの方位や距離を確認する欄を追加できるようにしました。宮崎空港から北へ出発する際の旋回案内も見直しました。

#### 変更

- チェックポイントの追加欄を地図の下に移し、保存前と保存後の地点を色で区別できるようにしました。経路の選択が必要な場面も分かりやすくしました。
- 機体の装備を選び、巡航速度の計算に反映できるようにしました。

### 開発者向け

- NAV LOG の VOR/DME 列を左側へ必要な数だけ追加し、列ごとに基準局を選択・削除できるようにしました。
- VOR/DME 基準局の候補を、出発飛行場、到着飛行場、経路に近い5局（出発飛行場から近い順）、その他の識別符号順で表示するようにしました。
- UMKまたはOMARUを通るRJFM北方面出発ガイダンスに旋回開始MZE DMEを表示し、4 DME未満の案内文と警告判定を廃止しました。
- ファイル取込後に経路選択が必須のとき、選択欄をブロッキング状態として赤く強調するようにしました。

- MAPとの往復を減らすため、チェックポイント追加パネルをMAPの下・経路表の上へ移動しました。
- 地図からチェックポイント座標を選ぶと、保存前は紫色の仮CP、保存後は茶色の確定CPとしてMAP上で区別できるようにしました。
- MAPの空域注記から、境界付近で管轄機関への確認を促す文を削除しました。
- チェックポイントから10 NM以内のLegを対応候補とし、1件なら自動選択、複数件なら関連Leg欄を表示するようにしました。
- RUN UPの下へノーズフェアリングの装備選択を追加しました。ノーズフェアリングありではPOH巡航速度、なしでは`-10 kt`とし、A/C ONの`-2 kt`を個別に適用します。ノーズフェアリングは既定OFFのため、A/CもONなら合計`-12 kt`です。

## 1.3.1

### 利用者向け

#### 修正

- 長い地点名や狭い画面で、入力欄やボタンがはみ出したり重なったりする問題を修正しました。
- チェックポイントの確定ボタンを、新規作成時は「追加」、編集時は「保存」と表示するようにしました。
- 訓練用の固定気象で、最初に入力される風の設定を見直しました。

### 開発者向け

- 長い関連Leg名を持つチェックポイント追加フォームが経路パネルからはみ出し、追加・保存ボタンが見えなくなる表示崩れを修正しました。
- チェックポイント新規作成時の確定ボタンを「追加」、編集中は「保存」と表示し、操作結果が経路パネルへ反映されることを明確にしました。
- 横幅の狭い入力パネルではFROMとTOを縦並びにし、長い空港名がパネル外へはみ出さないようにしました。
- 横幅の狭い入力パネルではDATEとETDも縦並びにし、日付入力と時刻入力の枠が重ならないようにしました。
- FTD固定気象の初期値を、地上風360°・15 kt、5,000 ft風270°・30 ktに変更しました。

## 1.3.0

### 利用者向け

#### 改善

- 内部処理を改善しました。AutoNavLogの使い方に変更はありません。

### 開発者向け

- AutoNavLog の現行バージョンを `1.3.0` に更新し、Python・Web・配布 workflow のバージョン表記を揃えました。

## 1.2.1

### 利用者向け

#### 修正

- 画面の版番号が、実際に使っている版と一致するようにしました。
- 燃料や訓練回数、機体の設定の入力欄を整理しました。
- NAV LOGの合計距離・時間が、表示された区間の合計と一致するようにしました。

### 開発者向け

- 実行時の版番号をインストール済みPackage Metadataから取得するようにし、`/healthz` とWeb画面左上の版表示が `pyproject.toml` と一致するようにしました。
- 飛行計画入力で `FUEL gal` と `TGL`、`RUN UP あり` と `A/C ON` をそれぞれ横並びにし、チェックボックスとラベルを同一行へ整理しました。
- NAV LOGの親行 `ZONE / CUM` DIST・ETEを、表示済みの子区間を0.5 NM・0.5分単位で合算する表示へ修正しました。計算・監査用の未丸め値は変更していません。

## 1.2.0

### 利用者向け

#### 追加

- 取り込んだ経路から、出発地と到着地の空港を自動設定するようにしました。
- 出発前のエンジン確認とエアコンの使用有無を選べるようにし、必要な時間・燃料・速度に反映しました。燃料表には飛行に使う燃料の合計を追加しました。

#### 変更

- 気圧の入力をなくし、気象設定を整理しました。風向の入力も統一し、狭い画面では入力からNAV LOGまで順に進めるようにしました。

### 開発者向け

- KML候補の始点・終点から5 NM以内の空港をFROM/TOとして自動決定し、Webでは読取専用で表示するようにしました。
- RUN UPの有無とA/C ON/OFFを計画入力へ追加しました。RUN UPありは10分・1.5 galとして燃料計画へ反映します。
- FUEL表へBOF Fuelを追加しました。BOFは `CLIMB + CRUISE + DESCENT + TGL + ADDITIONAL` で、RUN UPとRESERVEは含みません。

- QNHの入力・取得・推定・表示・帳票出力を廃止し、標準気象モードをMSMへ統一しました。
- POH巡航表から自動採用したKTASへ、ノーズフェアリングなしの `-10 KTAS` と、A/C ON時の追加 `-2 KTAS` を適用するようにしました。手入力TAS/GPHには補正を重ねません。
- 風向入力を001〜360の整数に統一し、内部では北を0°へ正規化しつつ画面・帳票では360として表示します。
- 経路表のROLE列を削除しました。内部roleと地図上の表現は維持しています。
- 1,240 px以下の画面を「入力 → 経路・MAP → 準備状況 → NAV LOG → ガイダンス」の縦順へ統一しました。

- Project schemaをv2へ更新しました。旧Project/SnapshotのQNH fieldは読込時に破棄し、RUN UP・A/Cは既定ONとして移行します。
- 旧データの風向0°は北360°として読み込み、再保存時は新しい入力契約へ揃えます。

## 1.1.0

### 利用者向け

#### 変更

- 宮崎空港から北へ向かう経路の高度設定と旋回案内を見直し、NAV LOGを確認した後に案内を読めるようにしました。
- 宮崎空港周辺の空域を地図に追加し、狭い画面でも入力から計算結果まで順に確認できるようにしました。
- 出発地と目的地の気温の取得方法を見直しました。取得できない場合は「未取得」と表示します。

### 開発者向け

- RJFMから大分方面への北行き例外で、NAV LOG上の `RJFM → OMARU` を1つの親Legとし、
  `UMK/RCA 5,500 ft` をその内部境界として表示するようにしました。経路表とRoute Graphは
  UMK・OMARUを個別に保持します。
- RJFMからUMKのALT・Phaseを `UMK 5,500 ft HIT / CLIMB`、UMKからOMARUを
  `5,500 ft / CRUISE`に固定しました。固定区間の入力欄は表示せず、OMARUから先は編集できます。
- RWY09の延長旋回は左、RWY27の初期旋回と延長旋回は右にしました。旋回案内の保存形式は
  `turn_direction`、`full_turns`、`partial_turn_deg` の方向非依存の名前に変更しました。
- RJFM案内カードをNAV LOG主表・FUEL表の下へ移し、現在の計算結果だけで表示するように
  しました。MAPのRJFM経路線と凡例はそのまま表示します。
- 画面幅1,240 px以下を「入力 → 経路・MAP → 準備状況 → NAV LOG → RJFM案内」の1列順にし、
  操作中の上下往復を減らしました。
- RJFM案内カードをNorth Up上の出発方位に合わせてRWY27を左、RWY09を右へ配置し、
  表示項目を旋回開始高度とNAV LOG直線Legとの差へ整理しました。旋回モデルの前提は
  カード共通説明として表示します。
- RJFMのMAPへ宮崎特別管制区（PCA）境界、9 km中心除外円、国土地理院の民間訓練試験空域
  KS4を追加しました。KS4はライブの表示専用レイヤーで、NAV LOG計算やPCA判定には使いません。
- 出発地・目的地のTOATは、空港標高を上空気圧面へ外挿せず、MSM地上気温から取得するように
  しました。取得できない場合は従来どおり未取得を明示します。
- 開発用Composeの既定bind addressを `0.0.0.0` に変更しました。Cloudflare Tunnel用originは
  起動時に `127.0.0.1` を明示してloopbackへ限定します。

- 旧Projectの `full_left_turns` と `partial_left_turn_deg` は読込時に左旋回として移行します。
  旧参照パックの案内結果は現行計算とは扱わず、新しい参照パックで再計算します。

## 1.0.0

### 利用者向け

#### 追加

- 初回の正式版です。訓練用に風を指定して計算できるようにしました。
- 地図や緯度・経度からチェックポイントを追加・編集・削除できるようにしました。パソコンでは地図の高さも変更できます。

#### 変更

- モバイルでは地図の確認と経路確定を地図のすぐ下に置き、入力後に上へ戻らず進めるようにしました。

### 開発者向け

- AutoNavLogの初回正式版です。従来のNAV2計算・安全ゲートに加え、FTD訓練と
  Web上のCheck Point計画、端末幅に応じた地図操作を正式版の対象に含めます。
- FTD 固定気象を追加しました。地上風と 5,000 ft の風を入力すると、0〜5,000 ft は
  風ベクトルを線形補間し、気温は各高度の標準大気で計算します。
- Web 地図または緯度・経度から Check Point を追加し、名称、関連 Leg、座標の編集と削除を
  行えるようにしました。abeam 点、Leg 内距離、累積距離、横ずれも計算前に確認できます。
- PC の3列表示で、地図の下端をドラッグまたはキーボード操作して高さを変更できるように
  しました。

- モバイルでは、地図の確認と経路確定を地図直下に配置しました。飛行計画の入力後、上へ
  戻らず下方向へ進めます。

## 0.3.1

### 利用者向け

#### 追加

- 保存済みの経路の削除、飛行計画の名前の変更、計算中の進み具合の表示に対応しました。

#### 変更

- 燃料を消して入力し直せるようにしました。また、説明書から起動・更新方法などを探しやすくしました。

#### 修正

- 保存した飛行計画のフォルダーを開くボタンが、必要な場面で表示されない問題を修正しました。

### 開発者向け

- 保存済みの経路を画面から削除できるようにしました。自動削除の処理も追加しています。
- NAV LOG の計算中に進捗を表示するオーバーレイを追加しました。
- Project 名を作成後に変更できるようにしました。

- 燃料欄を編集中、一時的に空欄にできるようにしました。空欄のままでは計算できません。
- README の順序と表現を見直し、起動、更新、安全上の制限を探しやすくしました。

- 保存済み Project のフォルダーを開くボタンが、表示すべき場面で隠れる問題を修正しました。

## 0.3.0

### 利用者向け

#### 追加

- ブラウザーから経路の取り込み、飛行計画の入力・保存、NAV LOGの計算、A4転記補助表の出力ができるようにしました。
- 気象情報を使った計算と、目的地の天気予報の表示に対応しました。

#### 修正

- 降下を始める位置や、計算に使う気温・機体性能の選び方を修正し、目的地の予報が有効か確認するようにしました。

### 開発者向け

- Docker で動く React/FastAPI 版を追加しました。
- KML/KMZ の取込、飛行計画の入力、NAV LOG 計算、Project 保存、A4 転記補助表の出力を
  Web 画面から行えるようにしました。
- MSM と METAR を使う QNH 推定、目的地 TAF の参考表示、Cloudflare Access による利用者識別を
  追加しました。
- 旧 Colab 版の自己検証付き配布と、実 MSM のリリース受入検査を追加しました。

- 変針点をまたぐ EOC の計算を修正しました。
- 上昇、巡航、降下で使う TOAT と巡航性能の選択を修正しました。
- 目的地 TAF の有効時刻、取得期限、同時取得数を検査するようにしました。
