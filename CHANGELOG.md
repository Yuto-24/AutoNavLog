# 変更履歴

AutoNavLog の利用者に影響する変更を記録します。

## Unreleased

## 1.3.2 - 2026-08-21

### 追加

- NAV LOG の VOR/DME 列を左側へ必要な数だけ追加し、列ごとに基準局を選択・削除できるようにしました。
- VOR/DME 基準局の候補を、出発飛行場、到着飛行場、経路に近い5局（出発飛行場から近い順）、その他の識別符号順で表示するようにしました。
- UMKまたはOMARUを通るRJFM北方面出発ガイダンスに旋回開始MZE DMEを表示し、4 DME未満の案内文と警告判定を廃止しました。
- ファイル取込後に経路選択が必須のとき、選択欄をブロッキング状態として赤く強調するようにしました。

### 変更

- MAPとの往復を減らすため、チェックポイント追加パネルを経路・MAPの上へ移動しました。

## 1.3.1 - 2026-08-20

### 修正

- 長い関連Leg名を持つチェックポイント追加フォームが経路パネルからはみ出し、追加・保存ボタンが見えなくなる表示崩れを修正しました。
- チェックポイント新規作成時の確定ボタンを「追加」、編集中は「保存」と表示し、操作結果が経路パネルへ反映されることを明確にしました。
- 横幅の狭い入力パネルではFROMとTOを縦並びにし、長い空港名がパネル外へはみ出さないようにしました。
- 横幅の狭い入力パネルではDATEとETDも縦並びにし、日付入力と時刻入力の枠が重ならないようにしました。
- FTD固定気象の初期値を、地上風360°・15 kt、5,000 ft風270°・30 ktに変更しました。

## 1.3.0 - 2026-08-20

### 変更

- AutoNavLog の現行バージョンを `1.3.0` に更新し、Python・Web・配布 workflow のバージョン表記を揃えました。

## 1.2.1 - 2026-08-17

### 修正

- 実行時の版番号をインストール済みPackage Metadataから取得するようにし、`/healthz` とWeb画面左上の版表示が `pyproject.toml` と一致するようにしました。
- 飛行計画入力で `FUEL gal` と `TGL`、`RUN UP あり` と `A/C ON` をそれぞれ横並びにし、チェックボックスとラベルを同一行へ整理しました。
- NAV LOGの親行 `ZONE / CUM` DIST・ETEを、表示済みの子区間を0.5 NM・0.5分単位で合算する表示へ修正しました。計算・監査用の未丸め値は変更していません。

## 1.2.0 - 2026-08-17

### 追加

- KML候補の始点・終点から5 NM以内の空港をFROM/TOとして自動決定し、Webでは読取専用で表示するようにしました。
- RUN UPの有無とA/C ON/OFFを計画入力へ追加しました。RUN UPありは10分・1.5 galとして燃料計画へ反映します。
- FUEL表へBOF Fuelを追加しました。BOFは `CLIMB + CRUISE + DESCENT + TGL + ADDITIONAL` で、RUN UPとRESERVEは含みません。

### 変更

- QNHの入力・取得・推定・表示・帳票出力を廃止し、標準気象モードをMSMへ統一しました。
- POH巡航表から自動採用したKTASへ、ノーズフェアリングなしの `-10 KTAS` と、A/C ON時の追加 `-2 KTAS` を適用するようにしました。手入力TAS/GPHには補正を重ねません。
- 風向入力を001〜360の整数に統一し、内部では北を0°へ正規化しつつ画面・帳票では360として表示します。
- 経路表のROLE列を削除しました。内部roleと地図上の表現は維持しています。
- 1,240 px以下の画面を「入力 → 経路・MAP → 準備状況 → NAV LOG → ガイダンス」の縦順へ統一しました。

### 互換性

- Project schemaをv2へ更新しました。旧Project/SnapshotのQNH fieldは読込時に破棄し、RUN UP・A/Cは既定ONとして移行します。
- 旧データの風向0°は北360°として読み込み、再保存時は新しい入力契約へ揃えます。


## 1.1.0 - 2026-08-17

### 変更

- RJFMから大分方面への北行き例外で、NAV LOG上の `RJFM → OMARU` を1つの親Legとし、
  `UMK/RCA 5,500 ft` をその内部境界として表示するようにしました。経路表とRoute Graphは
  UMK・OMARUを個別に保持します。
- RJFMからUMKのALT・Phaseを `UMK 5,500 ft HIT / CLIMB`、UMKからOMARUを
  `5,500 ft / CRUISE`に固定しました。固定区間の入力欄は表示せず、OMARUから先は編集できます。
- RWY09の延長旋回は左、RWY27の初期旋回と延長旋回は右にしました。旋回案内の保存形式は
  `turn_direction`、`full_turns`、`partial_turn_deg` の方向非依存の名前に変更しました。
- RJFM案内カードをNAV LOG主表・FUEL表の下へ移し、現在の計算結果だけで表示するように
  しました。MAPのRJFM経路線と凡例はそのまま表示します。
- 画面幅1,240 px以下を「入力 → 経路・MAP → 準備状況 → NAV LOG → RJFM案内」の1列順にし、
  操作中の上下往復を減らしました。
- RJFM案内カードをNorth Up上の出発方位に合わせてRWY27を左、RWY09を右へ配置し、
  表示項目を旋回開始高度とNAV LOG直線Legとの差へ整理しました。旋回モデルの前提は
  カード共通説明として表示します。
- RJFMのMAPへ宮崎特別管制区（PCA）境界、9 km中心除外円、国土地理院の民間訓練試験空域
  KS4を追加しました。KS4はライブの表示専用レイヤーで、NAV LOG計算やPCA判定には使いません。
- 出発地・目的地のTOATは、空港標高を上空気圧面へ外挿せず、MSM地上気温から取得するように
  しました。取得できない場合は従来どおり未取得を明示します。
- 開発用Composeの既定bind addressを `0.0.0.0` に変更しました。Cloudflare Tunnel用originは
  起動時に `127.0.0.1` を明示してloopbackへ限定します。

### 互換性

- 旧Projectの `full_left_turns` と `partial_left_turn_deg` は読込時に左旋回として移行します。
  旧参照パックの案内結果は現行計算とは扱わず、新しい参照パックで再計算します。

## 1.0.0 - 2026-08-16

AutoNavLogの初回正式版です。従来のNAV2計算・安全ゲートに加え、FTD訓練と
Web上のCheck Point計画、端末幅に応じた地図操作を正式版の対象に含めます。

### 追加

- FTD 固定気象を追加しました。地上風と 5,000 ft の風を入力すると、0〜5,000 ft は
  風ベクトルを線形補間し、気温は各高度の標準大気で計算します。
- Web 地図または緯度・経度から Check Point を追加し、名称、関連 Leg、座標の編集と削除を
  行えるようにしました。abeam 点、Leg 内距離、累積距離、横ずれも計算前に確認できます。
- PC の3列表示で、地図の下端をドラッグまたはキーボード操作して高さを変更できるように
  しました。

### 変更

- モバイルでは、地図の確認と経路確定を地図直下に配置しました。飛行計画の入力後、上へ
  戻らず下方向へ進めます。

## 0.3.1 - 2026-08-16

### 追加

- 保存済みの経路を画面から削除できるようにしました。自動削除の処理も追加しています。
- NAV LOG の計算中に進捗を表示するオーバーレイを追加しました。
- Project 名を作成後に変更できるようにしました。

### 変更

- 燃料欄を編集中、一時的に空欄にできるようにしました。空欄のままでは計算できません。
- README の順序と表現を見直し、起動、更新、安全上の制限を探しやすくしました。

### 修正

- 保存済み Project のフォルダーを開くボタンが、表示すべき場面で隠れる問題を修正しました。

## 0.3.0 - 2026-08-15

### 追加

- Docker で動く React/FastAPI 版を追加しました。
- KML/KMZ の取込、飛行計画の入力、NAV LOG 計算、Project 保存、A4 転記補助表の出力を
  Web 画面から行えるようにしました。
- MSM と METAR を使う QNH 推定、目的地 TAF の参考表示、Cloudflare Access による利用者識別を
  追加しました。
- 旧 Colab 版の自己検証付き配布と、実 MSM のリリース受入検査を追加しました。

### 修正

- 変針点をまたぐ EOC の計算を修正しました。
- 上昇、巡航、降下で使う TOAT と巡航性能の選択を修正しました。
- 目的地 TAF の有効時刻、取得期限、同時取得数を検査するようにしました。
